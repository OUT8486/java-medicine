# 🚀 药店管理系统 - 快捷启动指南

## 📋 目录

1. [快速开始](#快速开始)
2. [环境要求](#环境要求)
3. [启动脚本说明](#启动脚本说明)
4. [手动启动方式](#手动启动方式)
5. [常见问题](#常见问题)

---

## 🎯 快速开始

### ✅ 一键启动（推荐）

**直接双击运行 `start.bat` 文件即可自动启动所有服务！**

系统会自动按顺序启动：
1. Redis 缓存服务
2. MySQL 数据库服务
3. Spring Boot 后端服务（端口 8080）
4. Vue 前端服务（端口 5173）

等待约 10-15 秒后，浏览器会自动打开 http://localhost:5173

---

## 🔧 环境要求

在启动系统前，请确保已安装以下软件：

### 必需软件

- **Java JDK 21+**
  - 下载地址：https://www.oracle.com/java/technologies/downloads/
  - 验证命令：`java -version`

- **Node.js 16+ & npm**
  - 下载地址：https://nodejs.org/
  - 验证命令：`node --version` 和 `npm --version`

- **MySQL 8.0+**
  - 确保 MySQL 服务正在运行
  - 创建数据库：`CREATE DATABASE medicine CHARACTER SET utf8mb4;`
  - 导入 SQL 脚本：`creat_table.sql` 和 `insert_date.sql`

- **Redis 7.0+**
  - Windows 版本下载地址：https://github.com/microsoftarchive/redis/releases
  - 或使用 Docker：`docker run -d -p 6379:6379 redis:latest`
  - 验证命令：`redis-cli ping`（应返回 PONG）

- **Maven 3.6+**
  - 通常随 IDE 自带，也可单独安装
  - 验证命令：`mvn -version`

### 配置文件检查

**1. 数据库配置**

编辑 `src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medicine?...
    username: root      # 修改为你的 MySQL 用户名
    password: 123456    # 修改为你的 MySQL 密码
```

**2. Redis 配置**

编辑 `src/main/resources/application.yml`：
```yaml
spring:
  data:
    redis:
      host: localhost        # Redis 服务器地址
      port: 6379            # Redis 端口
      password: 123456      # Redis 密码（如无密码可删除此行）
      database: 0           # 使用的数据库编号
```

**注意**：如果使用远程 Redis 服务器，请确保：
- Redis 允许外部连接（修改 `redis.conf` 中的 `bind` 和 `protected-mode`）
- 防火墙开放 6379 端口
- 网络连接正常

---

## 📜 启动脚本说明

项目提供了两个便捷的批处理脚本：

### start.bat - 一键启动所有服务

**功能**：
- ✅ 检查 Java、Node.js、Redis 环境
- ✅ 自动启动 Redis 服务（如未运行）
- ✅ 启动 Spring Boot 后端服务
- ✅ 智能等待后端就绪
- ✅ 启动 Vue 前端服务
- ✅ 自动打开浏览器访问系统

**使用方法**：
```
双击 start.bat 文件
```

### stop.bat - 一键停止所有服务

**功能**：
- ✅ 停止后端服务（端口 8080）
- ✅ 停止前端服务（端口 5173）
- ✅ 清理相关进程

**使用方法**：
```
双击 stop.bat 文件
```

---

## 🔧 手动启动方式

如果需要手动控制每个服务的启动，请按以下顺序操作：

### 1. 启动 Redis 服务

**Windows 本地 Redis**：
```bash
# 如果已安装为服务
net start Redis

# 或者手动启动
redis-server.exe
```

**Docker 方式**：
```bash
docker run -d -p 6379:6379 --name redis redis:latest
```

**验证 Redis 是否启动成功**：
```bash
redis-cli ping
# 应该返回：PONG
```

### 2. 启动 MySQL 数据库

确保 MySQL 服务正在运行：
```bash
# Windows
net start MySQL80

# 或者通过服务管理器启动 MySQL 服务
```

**初始化数据库**（首次运行时）：
```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE medicine CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出 MySQL
exit

# 导入表结构
mysql -u root -p medicine < creat_table.sql

# 导入初始数据
mysql -u root -p medicine < insert_date.sql
```

### 3. 启动后端服务

打开命令提示符或 PowerShell，执行：

```bash
cd "C:\Users\Lenovo\Desktop\药店管理系统"
mvn spring-boot:run
```

看到以下日志表示启动成功：
```
Tomcat started on port(s): 8080 (http)
Started Application in X.XXX seconds
```

**注意**：如果 Redis 未启动，应用会自动降级为直接查询数据库模式，但性能会下降。

### 4. 启动前端服务

打开**新的**命令提示符或 PowerShell，执行：

```bash
cd "C:\Users\Lenovo\Desktop\药店管理系统\vue-medicine"
npm install  # 首次运行需要安装依赖
npm run dev
```

看到以下日志表示启动成功：
```
VITE v8.0.1  ready in XXX ms
➜  Local:   http://localhost:5173/
```

### 5. 访问系统

浏览器打开：http://localhost:5173

---

## ❓ 常见问题

### Q1: 双击 start.bat 后没有任何反应？

**可能原因**：
- 缺少 Java 或 Node.js 环境
- 环境变量未配置

**解决方法**：
1. 打开 cmd，执行 `java -version` 和 `node --version`
2. 如果报错，请先安装对应环境
3. 确保环境变量 PATH 中包含 Java 和 Node.js 的路径

### Q2: 启动后提示"端口已被占用"？

**原因**：之前的服务未完全停止

**解决方法**：
```bash
# 方法 1：使用停止脚本
双击 stop.bat

# 方法 2：手动查找并终止进程
netstat -ano | findstr :8080
taskkill /PID <找到的 PID> /F

# 方法 3：重启电脑
```

### Q3: Redis 连接失败？

**症状**：后端启动时出现 Redis 连接错误

**解决方法**：
1. 确认 Redis 服务已启动：`redis-cli ping`
2. 检查 `application.yml` 中的 Redis 配置是否正确
3. 如果使用远程 Redis，检查网络和防火墙设置
4. 查看 Redis 日志排查问题

**临时方案**：如果 Redis 暂时无法使用，应用会自动降级为直接查询数据库，但性能会受影响。

### Q4: 前端页面无法访问？

**检查步骤**：
1. 确认前端服务已启动（查看是否有 "Local: http://localhost:5173" 日志）
2. 检查防火墙是否阻止
3. 尝试访问 http://127.0.0.1:5173
4. 清除浏览器缓存（Ctrl + F5 强制刷新）

### Q5: 登录后提示"网络错误"？

**可能原因**：
- 后端服务未启动
- 后端 API 地址配置错误
- CORS 跨域问题

**解决方法**：
1. 确认后端服务正在运行（访问 http://localhost:8080/api/auth/login 测试）
2. 检查 `vue-medicine/src/utils/request.js` 中的 baseURL 配置
3. 查看后端控制台的错误日志

### Q6: 如何彻底停止服务？

**方式 1**（推荐）：
```
双击 stop.bat
```

**方式 2**（手动）：
```bash
# 查找并终止后端进程
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# 查找并终止前端进程
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# 或者直接在服务窗口按 Ctrl+C
```

### Q7: 如何验证 Redis 缓存是否生效？

**方法 1：查看后端日志**
- 第一次查询：会看到 SQL 语句输出（表示从数据库查询）
- 第二次查询：没有 SQL 输出（表示命中缓存）

**方法 2：使用性能测试接口**
```bash
# 测试所有模块的缓存性能
GET http://localhost:8080/api/performance-test/test-all

# 测试单个模块（如药品）
GET http://localhost:8080/api/performance-test/test/drug
```

响应会显示数据库查询时间和缓存查询时间的对比。

---

## 🎨 使用技巧

### 1. 固定到任务栏

将 `start.bat` 右键 → "发送到" → "桌面快捷方式"，然后重命名为"启动药店系统"

### 2. 开机自启动（可选）

将 `start.bat` 的快捷方式放入：
```
C:\Users\你的用户名\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
```

### 3. 查看服务日志

- **后端日志**：在"药店管理系统 - 后端服务"窗口中查看
- **前端日志**：在"药店管理系统 - 前端服务"窗口中查看
- **Redis 日志**：查看 Redis 控制台输出
- **浏览器日志**：按 F12 打开开发者工具查看

### 4. 修改端口（如需要）

**后端端口**：
编辑 `src/main/resources/application.yml`：
```yaml
server:
  port: 8080  # 修改为你想要的端口
```

**前端端口**：
编辑 `vue-medicine/vite.config.js`：
```javascript
export default {
  server: {
    port: 5173  // 修改为你想要的端口
  }
}
```

**Redis 端口**：
编辑 `src/main/resources/application.yml`：
```yaml
spring:
  data:
    redis:
      port: 6379  # 修改为你想要的端口
```

---

## 📞 技术支持

如遇问题，请检查：

1. **数据库连接**：确保 MySQL 服务正在运行
2. **Redis 服务**：确保 Redis 服务正在运行（`redis-cli ping` 返回 PONG）
3. **依赖安装**：确保已执行 `npm install` 和 `mvn clean install`
4. **配置文件**：检查 `application.yml` 中的数据库和 Redis 连接信息
5. **浏览器兼容性**：推荐使用 Chrome、Edge 等现代浏览器

**性能优化提示**：
- Redis 缓存在小数据量环境下仍能带来 50%+ 的性能提升
- 生产环境中数据量越大，缓存优势越明显
- 建议保持 Redis 服务持续运行以获得最佳性能

---

## 🎉 开始使用

一切准备就绪！现在你可以：

1. 双击 `start.bat` 启动系统
2. 访问 http://localhost:5173
3. 使用默认账号登录（如：admin/123456）
4. 开始管理你的药店业务！

**祝你使用愉快！** 🚀

---

## 安全认证说明（重要）

系统登录已升级为 JWT + BCrypt：

1. **密码加密**：用户密码在数据库中以 BCrypt 哈希存储，登录时比对哈希，不再明文存储。
2. **令牌认证**：登录成功返回 JWT（有效期默认 24 小时），前端请求携带 `Authorization: Bearer <token>`；除登录、注册、健康检查外，所有 `/api/**` 接口都需要有效令牌。
3. **注册角色固定**：公开注册接口创建的账号角色固定为“用户”，管理员账号由管理员通过 `POST /api/users`（需管理员令牌）创建。
4. **种子账号**：`insert_date.sql` 中的 admin/out12 密码仍为 123456，但已是 BCrypt 哈希。**如果数据库之前已导入过旧版数据，需要重新执行 `insert_date.sql`（或手动把 users 表密码更新为 BCrypt 哈希），否则旧账号无法登录。**
5. **JWT 密钥**：生产环境请通过环境变量 `JWT_SECRET` 覆盖默认密钥（至少 32 字节）；数据库/Redis 密码通过 `SPRING_DATASOURCE_PASSWORD`、`SPRING_DATA_REDIS_PASSWORD` 等环境变量注入，避免硬编码。
