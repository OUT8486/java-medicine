# 🚀 药店管理系统 - 快捷启动指南

## 📋 目录

1. [快速开始](#快速开始)
2. [环境要求](#环境要求)
3. [启动脚本说明](#启动脚本说明)
4. [手动启动方式](#手动启动方式)
5. [常见问题](#常见问题)

---

## 🎯 快速开始

## 🔧 手动启动方式

### 1. 启动后端服务

打开命令提示符或 PowerShell，执行：

```bash
cd "C:\Users\Lenovo\Desktop\药店管理系统"
mvn spring-boot:run
```

看到以下日志表示启动成功：
```
Tomcat started on port(s): 8080 (http)
Started Application in X.XXX seconds
```

### 2. 启动前端服务

打开**新的**命令提示符或 PowerShell，执行：

```bash
cd "C:\Users\Lenovo\Desktop\药店管理系统\vue-medicine"
npm run dev
```

看到以下日志表示启动成功：
```
VITE v8.0.1  ready in XXX ms
➜  Local:   http://localhost:5173/
```

### 3. 访问系统

浏览器打开：http://localhost:5173

---

## ❓ 常见问题

### Q1: 双击 start.bat 后没有任何反应？

**可能原因**：
- 缺少 Java 或 Node.js 环境
- 环境变量未配置

**解决方法**：
1. 打开 cmd，执行 `java -version` 和 `node --version`
2. 如果报错，请先安装对应环境
3. 确保环境变量 PATH 中包含 Java 和 Node.js 的路径

### Q2: 启动后提示"端口已被占用"？

**原因**：之前的服务未完全停止

**解决方法**：
```bash
# 方法 1：使用停止脚本
双击 stop.bat

# 方法 2：手动查找并终止进程
netstat -ano | findstr :8080
taskkill /PID <找到的 PID> /F

# 方法 3：重启电脑
```

### Q3: 前端页面无法访问？

**检查步骤**：
1. 确认前端服务已启动（查看是否有 "Local: http://localhost:5173" 日志）
2. 检查防火墙是否阻止
3. 尝试访问 http://127.0.0.1:5173
4. 清除浏览器缓存（Ctrl + F5 强制刷新）

### Q4: 登录后提示"网络错误"？

**可能原因**：
- 后端服务未启动
- 后端 API 地址配置错误
- CORS 跨域问题

**解决方法**：
1. 确认后端服务正在运行（访问 http://localhost:8080/api/auth/login 测试）
2. 检查 `vue-medicine/src/utils/request.js` 中的 baseURL 配置
3. 查看后端控制台的错误日志

### Q5: 如何彻底停止服务？

**方式 1**（推荐）：
```
双击 stop.bat
```

**方式 2**（手动）：
```bash
# 查找并终止后端进程
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# 查找并终止前端进程
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# 或者直接在服务窗口按 Ctrl+C
```

---

## 🎨 使用技巧

### 1. 固定到任务栏

将 `start.bat` 右键 → "发送到" → "桌面快捷方式"，然后重命名为"启动药店系统"

### 2. 开机自启动（可选）

将 `start.bat` 的快捷方式放入：
```
C:\Users\你的用户名\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup
```

### 3. 查看服务日志

- **后端日志**：在"药店管理系统 - 后端服务"窗口中查看
- **前端日志**：在"药店管理系统 - 前端服务"窗口中查看
- **浏览器日志**：按 F12 打开开发者工具查看

### 4. 修改端口（如需要）

**后端端口**：
编辑 `src/main/resources/application.yml`：
```yaml
server:
  port: 8080  # 修改为你想要的端口
```

**前端端口**：
编辑 `vue-medicine/vite.config.js`：
```javascript
export default {
  server: {
    port: 5173  // 修改为你想要的端口
  }
}
```

---

## 📞 技术支持

如遇问题，请检查：

1. **数据库连接**：确保 MySQL 服务正在运行
2. **依赖安装**：确保已执行 `npm install` 和 `mvn clean install`
3. **配置文件**：检查 `application.yml` 中的数据库连接信息
4. **浏览器兼容性**：推荐使用 Chrome、Edge 等现代浏览器

---

## 🎉 开始使用

一切准备就绪！现在你可以：

1. 双击 `start.bat` 启动系统
2. 访问 http://localhost:5173
3. 使用默认账号登录（如：admin/123456）
4. 开始管理你的药店业务！

**祝你使用愉快！** 🚀
