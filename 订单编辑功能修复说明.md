# 🔧 采购/销售订单编辑功能修复说明

## ✅ 已修复的问题

### 1. 字段名错误
**问题**: 列表页面中使用了错误的字段名 `row.order_id`  
**修复**: 
- 采购订单：改为 `row.po_id`
- 销售订单：改为 `row.so_id`

### 2. 添加调试日志
为了方便排查问题，已在以下位置添加了详细的调试日志:

#### 前端日志
- **PurchaseOrderForm.vue** - 显示路由参数和加载的数据
- **SalesOrderForm.vue** - 显示路由参数和加载的数据
- **request.js** - 显示 HTTP 请求详情

#### 后端日志
- **PurchaseOrderController** - 显示接收的 ID 参数和查询结果
- **SalesOrderController** - 显示接收的 ID 参数和查询结果

---

## 🧪 如何测试修复效果

### 步骤 1: 重启后端服务
```bash
# 停止当前运行的后端服务 (Ctrl+C)
# 重新启动
mvn spring-boot:run
```

### 步骤 2: 刷新前端页面
1. 打开浏览器访问 http://localhost:5173
2. 按 F12 打开开发者工具
3. 切换到 Console 标签

### 步骤 3: 测试采购订单编辑
1. 点击左侧菜单"采购订单"
2. 在列表中选择一个订单
3. 点击"编辑"按钮
4. 观察 Console 中的日志输出

### 步骤 4: 测试销售订单编辑
1. 点击左侧菜单"销售订单"
2. 在列表中选择一个订单
3. 点击"编辑"按钮
4. 观察 Console 中的日志输出

---

## 📋 预期的日志输出

### 前端 Console 应该显示:
```
=== 编辑模式 ===
路由参数 ID: PO001 (或 SO001)
是否为编辑：true
[HTTP Request] GET http://localhost:8080/api/purchase-orders/PO001
[Request Params] undefined
API 返回数据：{code: 200, message: 'success', data: {...}}
表单数据已填充：{po_id: 'PO001', supplier_id: '...', ...}
```

### 后端 Console 应该显示:
```
========================================
=== 接收到查询请求，ID: PO001 ===
=== ID 长度：5 ===
=== 查询结果：找到订单，PO_ID: PO001 ===
========================================
```

---

## ⚠️ 如果仍然出现问题

请检查以下日志信息并告诉我:

1. **前端 Console 中的完整日志**
   - 路由参数 ID 的值是什么？
   - HTTP 请求的完整 URL 是什么？
   - API 返回的错误信息是什么？

2. **后端 Console 中的日志**
   - 接收到的 ID 参数是什么？
   - 查询结果是找到还是未找到？

3. **数据库验证**
   - 使用 MySQL Workbench 或 Navicat 连接数据库
   - 执行查询确认订单是否存在:
     ```sql
     -- 采购订单
     SELECT * FROM purchaseorder WHERE po_id = '你的订单 ID';
     
     -- 销售订单
     SELECT * FROM salesorder WHERE so_id = '你的订单 ID';
     ```

---

## 🎯 修复内容总结

| 文件 | 修改内容 | 目的 |
|------|---------|------|
| PurchaseOrderList.vue | row.order_id → row.po_id | 字段名修正 |
| SalesOrderList.vue | row.order_id → row.so_id | 字段名修正 |
| PurchaseOrderForm.vue | 添加调试日志 | 问题排查 |
| SalesOrderForm.vue | 添加调试日志 | 问题排查 |
| request.js | 添加请求日志 | 查看请求详情 |
| PurchaseOrderController.java | 添加详细日志 | 查看后端接收情况 |
| SalesOrderController.java | 添加详细日志 | 查看后端接收情况 |

---

## 📝 数据库表名确认

已确认数据库表名正确:
- ✅ 采购订单表：`purchaseorder` (无中划线)
- ✅ 销售订单表：`salesorder` (无中划线)
- ✅ Mapper 中的 SQL 语句使用正确的表名

---

**修改完成时间**: 2026-03-22  
**状态**: ✅ 已修复并添加调试日志，等待测试验证
