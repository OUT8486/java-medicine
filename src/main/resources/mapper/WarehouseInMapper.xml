<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_boot.dao.WarehouseInMapper">

    <!-- 入库记录结果映射 -->
    <resultMap id="WarehouseInResultMap" type="com.example.spring_boot.entity.WarehouseIn">
        <id property="record_id" column="record_id"/>
        <result property="drug_id" column="drug_id"/>
        <result property="warehouse_id" column="warehouse_id"/>
        <result property="quantity" column="quantity"/>
        <result property="batch_number" column="batch_number"/>
        <result property="production_date" column="production_date"/>
        <result property="expiry_date" column="expiry_date"/>
        <result property="purchase_price" column="purchase_price"/>
        <result property="in_date" column="in_date"/>
        <result property="operator_id" column="operator_id"/>
    </resultMap>

    <!-- 新增入库记录 -->
    <insert id="insertWarehouseIn" parameterType="com.example.spring_boot.entity.WarehouseIn">
        INSERT INTO warehouse_in (
            record_id,
            drug_id,
            warehouse_id,
            quantity,
            batch_number,
            production_date,
            expiry_date,
            purchase_price,
            in_date,
            operator_id
        ) VALUES (
            #{record_id},
            #{drug_id},
            #{warehouse_id},
            #{quantity},
            #{batch_number},
            #{production_date},
            #{expiry_date},
            #{purchase_price},
            #{in_date},
            #{operator_id}
        )
    </insert>

    <!-- 修改入库记录 -->
    <update id="updateWarehouseIn" parameterType="com.example.spring_boot.entity.WarehouseIn">
        UPDATE warehouse_in
        SET
            drug_id = #{drug_id},
            warehouse_id = #{warehouse_id},
            quantity = #{quantity},
            batch_number = #{batch_number},
            production_date = #{production_date},
            expiry_date = #{expiry_date},
            purchase_price = #{purchase_price},
            in_date = #{in_date},
            operator_id = #{operator_id}
        WHERE record_id = #{record_id}
    </update>

    <!-- 删除入库记录 -->
    <delete id="deleteWarehouseIn">
        DELETE FROM warehouse_in
        WHERE record_id = #{record_id}
    </delete>

    <!-- 根据ID查询入库记录 -->
    <select id="selectWarehouseInById" resultMap="WarehouseInResultMap">
        select * from warehouse_in
        where record_id = #{record_id}
    </select>

    <!-- 查询所有入库记录 -->
    <select id="selectAllWarehouseIns" resultMap="WarehouseInResultMap">
        select * from warehouse_in
        order by record_id
    </select>

</mapper>