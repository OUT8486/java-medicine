<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_boot.mapper.UserMapper">
    <select id="LoginUser" resultType="java.lang.String">
        SELECT role FROM users WHERE user_name = #{user_name} AND password = #{password}
    </select>

    <insert id="RegisterUser" parameterType="com.example.spring_boot.pojo.Users">
        INSERT INTO users (user_id, user_name, password, role)
        VALUES (#{user_id}, #{user_name}, #{password}, #{role})
    </insert>

    <select id="OutUser" resultType="java.lang.String">
        SELECT 'User logged out successfully' as result
    </select>

    <insert id="insertUser" parameterType="com.example.spring_boot.pojo.Users">
        INSERT INTO users (user_id, user_name, password, role)
        VALUES (#{user_id}, #{user_name}, #{password}, #{role})
    </insert>
</mapper>